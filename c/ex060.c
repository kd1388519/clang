#include <stdio.h>
main()
{
	char data[15] = "Language";
	char i=0;
	char* p_dat, ch;
	p_data = data;
	printf("\ndata[] =%s\n",p_data);
	printf("ŒŸõ•¶š‚ÍH");
	scanf("%s", &ch);
	while (ch == *(p_data + 1)) {
		if (ch == *(p_data + 1)) {
			printf("%d", i + 1);

		}
		i++
	}
}